## Purpose

See the `VISION.md` file!

## Rules

  - Never run `gleam build` or `gleam test` (you're in a container and these won't work)
  - Use `mise run build` to check if the code compiles
  - Use `mise run test` to check if the tests pass
